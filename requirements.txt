# Minimum bounds rather than exact pins: Streamlit Community Cloud currently
# builds on Python 3.14, and older releases of numpy/scipy/Pillow have no
# cp314 wheels (pip then tries to compile them from source and fails, since
# the build image has no zlib headers or Fortran compiler).
streamlit>=1.63.0
pandas>=2.3.3,<3
numpy>=2.5.1,<3
scipy>=1.18.1
Pillow>=12.1.1
pdf2image>=1.17.0
huggingface_hub>=0.34.0
